name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm install

      - name: Prebuild Android project
        run: npx expo prebuild --platform android --no-install --clean --non-interactive

      - name: Remove broken compose compiler plugin from pager-view
        run: |
          PAGER_GRADLE="node_modules/react-native-pager-view/android/build.gradle"
          if [ -f "$PAGER_GRADLE" ]; then
            # Удаляем любые упоминания compose-compiler-gradle-plugin
            sed -i '/compose-compiler-gradle-plugin/d' "$PAGER_GRADLE"
            # Удаляем применение плагина compose, если оно есть
            sed -i '/org.jetbrains.kotlin.plugin.compose/d' "$PAGER_GRADLE"
            echo "--- Modified $PAGER_GRADLE ---"
            grep -i "compose" "$PAGER_GRADLE" || echo "Compose completely removed"
          fi

      - name: Configure Gradle Memory
        run: |
          mkdir -p ~/.gradle
          echo "org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m" >> ~/.gradle/gradle.properties
          echo "org.gradle.daemon=false" >> ~/.gradle/gradle.properties

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build Release APK
        working-directory: android
        run: ./gradlew assembleRelease --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: SNotes-Release-APK
          path: android/app/build/outputs/apk/release/*.apk
